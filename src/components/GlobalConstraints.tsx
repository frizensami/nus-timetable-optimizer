import React, { useState, useEffect } from 'react'
import './Solver.css'
import { Segment, Button, Container, Divider } from 'semantic-ui-react'

/**
 * Responsible for setting constraints globally for all modules
 * */
export const GlobalConstraints: React.FC<{}> = () => {

    return <div>globalconstraints</div>
}
