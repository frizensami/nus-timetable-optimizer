module.exports = {
    // plugins: [{ plugin: require('@semantic-ui-react/craco-less') }],
    webpack: {
        alias: {
            /*
            "react": "preact/compat",
            "react-dom": "preact/compat",
            */
        }
    }
}
